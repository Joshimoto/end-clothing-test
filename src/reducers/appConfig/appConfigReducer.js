const initialState = {
    renderedBy: "client"
}

const appConfigReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default appConfigReducer;